import 'package:flutter/material.dart';

class BookmarkedAccounts extends StatelessWidget {
  final Key keyTwo;
  BookmarkedAccounts({this.keyTwo});
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
